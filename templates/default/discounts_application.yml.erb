server:
    port: <%= node.deploy[:discounts][:port] %>

spring:
    data:
        elasticsearch:
        	clusterNodes: "localhost:9300"
        	
logging:
    file: "<%= node.discounts[:log_path] %>/<%= node.discounts[:log_file] %>"