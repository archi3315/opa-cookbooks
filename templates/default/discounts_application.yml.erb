server:
    port: <%= node[:discounts][:port] %>

spring:
    data:
        elasticsearch:
            clusterNodes: "<%= node[:opsworks][:instance][:private_ip] %>:9300"
            clusterName: "<%= node[:elasticsearch][:cluster][:name] %>"

logging:
    file: "<%= node.discounts[:log][:path] %>/<%= node.discounts[:log][:file] %>"